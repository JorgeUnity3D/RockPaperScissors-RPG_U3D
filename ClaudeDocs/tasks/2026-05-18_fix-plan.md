# Fix Plan — Architecture Audit 2026-05-18

Tareas ordenadas por prioridad. Completar de arriba a abajo.
Estado: [ ] pendiente  [x] hecho  [~] en progreso

> ✅ Plan completo — todos los bloques resueltos. Verificado 2026-05-19.

---

## BLOQUE A — Integridad de datos (crítico, hacer primero)

- [x] **A1** `StatAttribute.AddModifier()` / `RemoveModifier()` — fix null guard, GetModifier devuelve primer match; save eliminado (resuelto por A2)
- [x] **A2** Refactor `NotificableField` — eliminado auto-save del setter; `SuppressContextUpdates` eliminado; save points explícitos añadidos en 6 managers + TreasureStepManager + CombatManager; `PersistenceService` y `GameManager.ConfirmNewGame` simplificados
- [x] **A3** `GameConsts` — `TRAINING_EXP_PER_LEVEL` y `LEVEL_PRICES_AUX` ya eran `IReadOnlyList<int>` — efectivamente inmutables. Sin cambio necesario.
- [x] **A4** `Player(Player)` copy constructor — nadie llama `new Player(player)` en todo el codebase. No existe ni se necesita. N/A.
- [x] **A5** `Player.CurrentHealth` / `CurrentEnergy` — resuelto por A2; NotificableField ya no auto-guarda, cambios en combate no escriben a disco.

---

## BLOQUE B — Bugs de juego (game-breaking)

- [x] **B1** Loop de avance de steps — `GameManager.OnCombatFinished()` incrementa `CurrentStepIndex`, recarga la escena combat si hay más steps, o vuelve a Town si era el último (o derrota).
- [x] **B2** `Training EXP` — `CombatManager.EndCombat()` llama `ApplyPendingTrainingExp()` y luego dispara `AppEvents.OnGameContextUpdated?.Invoke()`. EXP persiste correctamente.
- [x] **B3** `NPCStepManager` — usa `_player.CurrentHealth` (HP actual) y `_player.MaxHealth.TotalValue` (HP máximo). Correcto.
- [x] **B4** `TownData.LevelProgress` — guard `if (Level >= GameConsts.TRAINING_EXP_PER_LEVEL.Count) return 1f` en su getter.

---

## BLOQUE C — Arquitectura: managers

- [x] **C1** `TreasureStepManager` y `NPCStepManager` — ambos extienden `BaseManager` con `SetUp()`, `Subscribe()`, `UnSubscribe()` correctos.
- [x] **C2** `CreditsTimeCounterManager` — `Update()` eliminado; usa `StartCoroutine(TickCoroutine())` lanzado desde `Initialize()`.
- [x] **C3** `TownManager.GoToTownMenu()` — ya no llama `targetManager.Initialize()`; usa `ITownBuilding.OnMenuOpen()` en su lugar.
- [x] **C4** `TravelManager` — no referencia `CreditsTimeCounterManager`; se comunica vía `AppEvents.OnTravelRequested` → `CreditsTimeCounterManager` → `AppEvents.OnTravelConfirmed`.
- [x] **C5** `MainMenuManager` — `OnBackToMainMenu += BackToMainMenu` (no Initialize); `_loadGameUIController` recibe datos vía `InstanceGames()`; `PersistenceService` desacoplado mediante callback `OnGameListLoaded`.
- [x] **C6** `ScissorBonfireManager.ConfirmLevelUp()` — resuelto por A2; NotificableField ya no auto-guarda durante el loop; un solo `OnGameContextUpdated` al final.

---

## BLOQUE D — Arquitectura: UIControllers

- [x] **D1** `CreditsTimeCounterUIController` — event-driven vía `OnCreditsUpdated` / `OnTimeUpdated`; `Update()` eliminado.
- [x] **D2** `PaperTreeUIController.RestoreNodeState()` — mutación movida a `PaperTreeManager`; UIController solo recibe estado ya computado.
- [x] **D3** `TravelUIController` — dispara `AppEvents.OnTravelRequested?.Invoke(level)` directamente; callback eliminado.
- [x] **D4** `StablesUIController` / `StoneSmithyUIController` — disparan AppEvents; callbacks directos eliminados.
- [x] **D5** `TrainingHouseUIController` — recibe `bool canAffordByStats` pre-computado por `TrainingHouseManager.ComputeCanAfford()`.

---

## BLOQUE E — Calidad de código / naming

- [x] **E1–E2** `readonly` en GameConsts (cubierto en A3) + `var` → tipos explícitos en `ServiceLocator`, `UIService`, `RNGGenerator`, `HouseStat`.
- [x] **E3** `RPSEnums.cs` — `MapStepType`, `VignetteAnimation`, `ComicPageLayout` convertidos a SCREAMING_SNAKE_CASE.
- [x] **E4** `GameEnums.cs` — `[Description("Scisso")]` → `"Scissor"`.
- [x] **E5** `StoneSmithyButton` — campos `public` → `[SerializeField] private`.
- [x] **E6** `Player.cs` — `#endregion EVENTS` movido fuera del accessor `remove`.
- [x] **E7** `AppEvents.cs` — `OnCombatRoundResolved` eliminado (nunca se invocaba).
- [x] **E8** `BaseModifier.GetModifier<T>()` — devuelve primer match.
- [x] **E9** `BaseModifier.Level` setter — guard `if (value < 1) return` añadido.
- [x] **E10** Warnings obsoletos de `LibraryModifier` eliminados de `LibraryModifier.cs` y `GameEnums.cs`.
- [x] **E11** `Player.Attributes` — nota de alocación documentada.
- [x] **E12** Cacheo de `_player` en managers — evaluado caso por caso; patrón correcto y consistente.

---

## Dependencias clave

```
Todas resueltas. Ver commits fd392c7 (A), 70f2dba (C), 6a2914c (D), 95b15f1 (E).
```
