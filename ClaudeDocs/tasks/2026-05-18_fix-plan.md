# Fix Plan — Architecture Audit 2026-05-18

Tareas ordenadas por prioridad. Completar de arriba a abajo.
Estado: [ ] pendiente  [x] hecho  [~] en progreso

---

## BLOQUE A — Integridad de datos (crítico, hacer primero)

- [x] **A1** `StatAttribute.AddModifier()` / `RemoveModifier()` — fix null guard, GetModifier devuelve primer match; save eliminado (resuelto por A2)
- [x] **A2** Refactor `NotificableField` — eliminado auto-save del setter; `SuppressContextUpdates` eliminado; save points explícitos añadidos en 6 managers + TreasureStepManager + CombatManager; `PersistenceService` y `GameManager.ConfirmNewGame` simplificados
- [ ] **A3** `GameConsts` — añadir `readonly` a `TRAINING_EXP_PER_LEVEL` y `LEVEL_PRICES_AUX`
- [ ] **A4** `Player(Player)` copy constructor — implementar correctamente o marcar con `throw new NotSupportedException()`
- [ ] **A5** `Player.CurrentHealth` / `CurrentEnergy` — desacoplar de `NotificableField` para que los cambios en combate no disparen `File.WriteAllText` en cada tick (resuelto por A2 — ya no hay auto-save)

---

## BLOQUE B — Bugs de juego (game-breaking)

- [ ] **B1** Implementar loop de avance de steps en `StepManager` / `CombatManager`: tras `OnCombatFinished`, incrementar `CombatContext.CurrentStepIndex` y pasar al siguiente step o volver a Town si era el último
- [ ] **B2** `Training EXP` — asegurar que `ApplyPendingTrainingExp()` dispara `OnGameContextUpdated` para que el EXP ganado en combate se persista
- [ ] **B3** `NPCStepManager` — reemplazar el doble `MaxHealth` por el HP actual del jugador en el SetData de la barra de vida (necesita canal desde CombatManager)
- [ ] **B4** `TownData.LevelProgress` — añadir guard de bounds para evitar IndexOutOfRange al llegar al nivel máximo

---

## BLOQUE C — Arquitectura: managers

- [ ] **C1** `TreasureStepManager` y `NPCStepManager` — extender `BaseManager`, mover service resolution a `SetUp()`, subscripciones a `Subscribe()`/`UnSubscribe()`
- [ ] **C2** `CreditsTimeCounterManager` — reemplazar `Update()` por una coroutine; mover `StartTimeCounter()` y `SetData()` fuera de `SetUp()` (a `Initialize()`)
- [ ] **C3** `TownManager.GoToTownMenu()` — eliminar la llamada directa a `targetManager.Initialize()`; delegar al flujo de `GameManager` vía AppEvent o rediseñar la inicialización de sub-managers
- [ ] **C4** `TravelManager` — eliminar referencia directa a `CreditsTimeCounterManager`; comunicarse vía AppEvents (`OnRequestCredit`, `OnCreditsChanged`, etc.)
- [ ] **C5** `MainMenuManager` — tres fixes:
  - Eliminar `AppEvents.OnBackToMainMenu += Initialize` (Initialize no debe ser listener)
  - Renombrar `_loadGameUIController.Initialize()` → `SetData()` o equivalente correcto
  - Desacoplar callback de `PersistenceService` → el manager recibe la lista y la pasa al controller
- [ ] **C6** `ScissorBonfireManager.ConfirmLevelUp()` — batch los writes de NotificableField (suprimir contexto durante el loop, disparar un solo save al final)

---

## BLOQUE D — Arquitectura: UIControllers

- [ ] **D1** `CreditsTimeCounterUIController` — eliminar `Update()`; suscribirse a los AppEvents correctos (`OnCreditsUpdated`, `OnTimeUpdated`) y activar esos eventos desde el manager
- [ ] **D2** `PaperTreeUIController.RestoreNodeState()` — mover la mutación de `node.IsUnlocked` al manager o a la capa de datos; UIController solo recibe estado ya computado
- [ ] **D3** `TravelUIController` — reemplazar callback `OnTravelConfirmed` por `AppEvents.OnTravelConfirmed?.Invoke(level)`; también renombrar campo privado con prefijo `_`
- [ ] **D4** `StablesUIController` / `StoneSmithyUIController` — reemplazar callbacks directos por AppEvents equivalentes
- [ ] **D5** `TrainingHouseUIController` — mover lógica de affordability al manager; UIController recibe `bool canAfford` pre-computado por stat

---

## BLOQUE E — Calidad de código / naming

- [ ] **E1** Añadir `readonly` a `GameConsts.LEVEL_PRICES_AUX` (ya cubierto en A3, verificar que queda completo)
- [ ] **E2** Corregir `var` en 4 archivos: `ServiceLocator.cs`, `UIService.cs`, `RNGGenerator.cs`, `HouseStat.cs`
- [ ] **E3** `RPSEnums.cs` — convertir `MapStepType`, `VignetteAnimation`, `ComicPageLayout` a SCREAMING_SNAKE_CASE (implica actualizar todos los switch/usos)
- [ ] **E4** `GameEnums.cs` — corregir `[Description("Scisso")]` → `[Description("Scissor")]`
- [ ] **E5** `StoneSmithyButton` — cambiar campos `public` → `[SerializeField] private`
- [ ] **E6** `Player.cs` — corregir `#endregion EVENTS` mal colocado dentro del accessor `remove`
- [ ] **E7** `AppEvents.cs` — eliminar o implementar `OnCombatRoundResolved` (actualmente nunca se invoca)
- [ ] **E8** `BaseModifier.GetModifier<T>()` — corregir para que devuelva el primer match, no el último
- [ ] **E9** `BaseModifier.Level` setter — añadir guard `if (_level.Value < 1) return` para evitar index -1
- [ ] **E10** `LibraryModifier.cs` y `GameEnums.cs` — eliminar warnings obsoletos sobre el converter de LibraryModifier (ya está implementado)
- [ ] **E11** `Player.Attributes` — documentar la alocación por acceso; evaluar si cachear o cambiar a un método con nombre que haga evidente el coste
- [ ] **E12** Managers que cachean `_player = AppContext.Player` — evaluar caso por caso si simplificar a lectura directa (menor impacto, no bloqueante)

---

## Dependencias clave

```
A1 → B2 (training EXP solo tiene sentido persistir si AddModifier guarda)
A5 → B1 (el loop de steps necesita saber el HP real entre steps)
C1 → B1 (los step managers necesitan estar bien en BaseManager antes de ampliar el loop)
C2 → D1 (el manager y el controller de credits se reforman juntos)
C4 → D3-D4 (AppEvents de travel/stables/smithy se definen junto al refactor de managers)
E3 → muchos (cambiar enum casing es un refactor amplio, dejarlo para el final del bloque E)
```
